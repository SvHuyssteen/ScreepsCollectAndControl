import { prototypes, utils, constants, visual } from "/game";
import { findClosestBuildSpaceLooselySpaced } from "../helper/findClosestBuildSpace";

export function extentionLogic(Source) {
  //look at the area around the source for buildable terrain
}
